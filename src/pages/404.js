import React from "react"
import Layout from "../components/layout"

const NotFoundPage = () => (
  <Layout>
    <h1>NOT FOUND</h1>
    <p>These are not the drone you&apos;re looking for... move along.</p>
  </Layout>
)

export default NotFoundPage
